# 物资采购需求计划管理系统 — Render 免费云端部署指南

> 本指南帮助你在 **Render 免费平台** 部署系统，让员工通过固定网址长期访问，
> **无需本机开机、无需内网穿透**。
>
> ⚠️ 部署前请先阅读文末「重要注意事项」，特别是**免费版数据持久化限制**。

---

## 一、准备工作（2 分钟）

1. 准备一个常用邮箱（用于注册 Render 账号）。
2. 确认已登录 GitHub 账号（没有可免费注册一个）。
3. 本机安装 Git（右键"Git Bash Here"或 cmd 均可）。
   - 检查是否已安装：命令行输入 `git --version`

---

## 二、将部署包推送为 Git 仓库（3 分钟）

部署包目录：`C:\Users\YZ\Desktop\RenderDeploy`（已含全部现有数据）

打开命令行，依次执行：

```bash
cd /d C:\Users\YZ\Desktop\RenderDeploy
git init
git add .
git commit -m "init deploy"
```

然后在 GitHub 网页新建仓库（New repository），复制仓库地址，继续执行：

```bash
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

> 推不上去？先设置本机 Git 身份：
> ```
> git config --global user.name "你的用户名"
> git config --global user.email "你的邮箱"
> ```

---

## 三、在 Render 创建免费 Web 服务（3 分钟）

1. 打开 https://render.com → 点右上角 **Get Started** → 用邮箱/GitHub 注册登录。
2. 登录后进入 Dashboard，点 **New** → **Web Service**。
3. 选择你刚才推送的 GitHub 仓库。
   - 首次使用需点击 **Connect GitHub** 授权 Render 访问你的仓库。
4. 页面会自动读取 `render.yaml`，填入：
   - **Name**：`wuzi-caigou-system`（可改）
   - **Environment**：`Python 3`
   - **Build Command**：`pip install -r requirements.txt`
   - **Start Command**：`gunicorn app:app`
   - **Instance Type**：选择 **Free**（免费）
5. 点 **Create Web Service**，等待 2~5 分钟自动构建部署。

---

## 四、获取你的固定网址（30 秒）

部署成功后，页面顶部会显示：

```
https://wuzi-caigou-system.onrender.com
```

这就是系统的**固定公网网址**，员工在任何地方（手机/电脑/任意网络）打开即可访问登录使用。

- 默认账号：`admin` / `admin123`（登录后请立即修改密码）
- 网页控制台可随时查看运行日志、手动重启、重新部署。

---

## 五、后期更新系统（可选）

每次在本地改完代码后，重新推送即可自动部署：

```bash
git add .
git commit -m "更新说明"
git push
```

Render 检测到 `main` 分支更新会自动重新构建部署。

---

## ⚠️ 六、重要注意事项（务必阅读）

### 1. 免费版数据持久化限制（最关键）

Render **免费版 Web Service 的磁盘是临时的**：服务每次重启、休眠唤醒或重新部署时，
`data/system.db` 会被**重置为上次部署打包时的状态**，此后新增的需求单、修改等**会丢失**。

也就是说：**免费方案下，SQLite 数据无法长期持久保存**。

### 2. 配套方案（建议选其一）

- **方案 A（推荐，稳定）**：把数据库迁到免费的云端数据库（如 Supabase/Neon 的 PostgreSQL，
  有免费额度且持久化），系统改造为连接远程数据库。这样数据永久保存，一劳永逸。
- **方案 B（省事，需定期手动备份）**：先用本方案跑起来，每次部署前把最新数据更新到
  部署包再推送；或在系统导出功能中定期导出 Excel 备份。数据量小、操作频繁时够用。
- **方案 C（付费升级）**：Render 付费实例约 $7/月起，可挂载**持久磁盘**，SQLite 数据可长期保存。

### 3. 免费额度

- 免费 Web Service：750 小时/月（约每天 25 小时配额，通常够用）。
- 长时间无访问会**休眠**，下一次访问会**自动唤醒**（约等待 30~60 秒）。
- 每月有 750 小时运行上限，超出后当月无法访问，需等额度重置或升级。

### 4. 国内访问速度

Render 服务器在海外，国内访问速度**偏慢**但一般可用。若单位要求内网级速度，
后续可考虑国内平台（腾讯云 CloudBase 等，需要付费或腾讯云账号）。

---

## 七、数据库已迁移到云端 PostgreSQL（Neon）

> ✅ 本部署包已升级为 **PostgreSQL 兼容层架构**：
> - `db.py` 自动根据环境变量 `DATABASE_URL` 决定使用 PostgreSQL 还是本地 SQLite。
> - 现有数据（用户、物料、需求单、明细、日志等）**已全部迁移到 Neon 云端数据库**。
> - 云端数据**持久保存**，服务重启/休眠/重新部署**不再丢失**。

### 重新部署时务必配置环境变量

在 Render 的 Web Service 页面：

1. 左侧进入 **Environment**（环境变量）标签页。
2. 点 **Add Environment Variable**。
3. 填写：
   - **Key**：`DATABASE_URL`
   - **Value**：你 Neon 项目的连接串（形如 `postgresql://user:pass@host/neondb?sslmode=require`）
4. 保存后，点 **Deploy**（或 **Manual Deploy → Clear build cache & Deploy**）重新部署。

> ⚠️ **不配置 `DATABASE_URL` 时**，系统会回退到本地 SQLite（免费版磁盘临时，数据会丢）。**务必配置**后才能持久化。

### 后期更新系统

改完代码后，重新上传/推送即可自动部署；数据库数据不受影响。
